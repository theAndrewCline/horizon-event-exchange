# Generator Package

The generator package provides utilities for generating events and test data for the Horizon Event Exchange system.

## Scripts
- `dev`: Start development server with hot reload
- `build`: Compile to executable binary

## Usage
Run the generator:
```bash
bun run dev
```

Build for production:
```bash
bun run build
```

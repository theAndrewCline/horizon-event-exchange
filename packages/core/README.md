# Core Package

The core package provides shared types, schemas, and utilities for the Horizon Event Exchange system.

## Usage
This package is consumed by other packages in the monorepo and contains:
- Shared type definitions
- Validation schemas
- Common utilities
